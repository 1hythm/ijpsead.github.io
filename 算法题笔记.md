# 算法题笔记 · 顺序表 & 链表

> 📅 整理自「算法题.docx」| 🏷️ 考研 408 · 数据结构 | 📅 2026-08-02

---

## 目录

- [第一部分：顺序表](#第一部分顺序表)
  - [一、暴力解法](#一暴力解法)
  - [二、快速排序](#二快速排序)
  - [三、优化解法](#三优化解法)
    - [1. 折半查找](#1-折半查找)
    - [2. 散列表](#2-散列表)
    - [3. 预处理](#3-预处理)
    - [4. 多指针法](#4-多指针法)
- [第二部分：链表](#第二部分链表)
  - [一、链表与顺序表的比较](#一链表与顺序表的比较)
  - [二、链表的定义](#二链表的定义)
  - [三、链表的基本操作](#三链表的基本操作)
  - [四、链表的暴力解](#四链表的暴力解)
  - [五、链表的优化解](#五链表的优化解)
  - [六、逆置](#六逆置)
  - [七、经典真题](#七经典真题)
- [总结速查表](#总结速查表)

---

## 第一部分：顺序表

### 一、暴力解法

两种基本暴力思路：

| 方法 | 说明 |
|------|------|
| **枚举** | 遍历所有可能情况，逐个检查 |
| **快速排序** | 排序后再处理（见下文） |

---

### 二、快速排序

#### 升序实现

```cpp
void Qsort(int A[], int L, int R){
    if (R < L)		return;                  // 区间内元素个数为0或1，直接递归返回即可
    int i = L, j = R;
    int pivot = A[L];
    while (i < j){
        while(i < j && A[j] >= pivot)	j--;
        while(i < j && A[i] <= pivot)	i++;
        if(i < j)	swap(A[i], A[j]);
    }
    swap(A[L], A[i]);
    Qsort(A, L, i-1);
    Qsort(A, i+1, R);
}
```

#### 降序变体

```cpp
while(i < j && A[j] <= pivot)	j--;   // 从右往左找比 pivot 大的
while(i < j && A[i] >= pivot)	i++;   // 从左往右找比 pivot 小的
```

#### ⚠️ 使用限制

- 题目明示或暗示**不能改变元素下标**时不能用
- 要求使用**稳定的排序算法**时不能用（快排不稳定）

> 平均 O(n log n)，最坏 O(n²)，空间 O(log n)，不稳定。

---

### 三、优化解法

四种核心优化思想速览：

| 方法 | 适用条件 | 时间复杂度 | 空间复杂度 |
|------|----------|:----------:|:----------:|
| 折半查找 | 有序顺序表 | O(log n) | O(1) |
| 散列表 | 数据范围有限 | O(n) | O(m) |
| 预处理 | 频繁区间查询 | O(n) 预处理 + O(1) 查询 | O(n) |
| 多指针法 | 线性表（主要是有序） | 视情况 | O(1) |

---

#### 1. 折半查找

> **核心要素**：有序顺序表
> **复杂度**：时间 O(log n)，空间 O(1)
> 有两种模板。

**模板一：标准二分查找**（`while (L <= R)`）

```cpp
int B_Search(int A[], int L, int R, int x){
    int mid;
    while(L <= R){                       // 只要区间不为空就继续
        mid = (L + R) / 2;
        if(A[mid] == x){
            return mid;                  // 查找成功
        }
        else if (A[mid] > x){
            R = mid - 1;
        }
        else    L = mid + 1;
    }
    return -1;                           // 查找失败
}
```

**模板二：边界查找**（`while (L < R)`）

```cpp
int B_Search(int A[], int L, int R, int x){
    int mid;
    while(L < R){                        // 缩小区间直到只有一个元素
        mid = (L + R) / 2;
        if (A[mid] >= x)	R = mid;     // 第一个符合条件的在 L~mid
        else    L = mid + 1;             // 第一个符合条件的在 mid+1~R
    }
    return L;                            // 可能成功也可能失败，需额外判断
}
```

> 🎯 **模板二适用场景**：在升序数组中查找**第一个等于 x** 的元素（数组中可能存在多个 x）。找到 pos 后需额外判断：`return (A[pos] == x) ? pos : -1;`

---

#### 2. 散列表

> **核心思想**：以空间换时间。创建辅助数组 `count[]`，`count[i]` 记录数值 i 出现的相关信息。
> **使用条件**：数据范围有限，可以用数组下标直接表示数据的值
> **复杂度**：时间 O(n)，空间 O(m)，其中 m 为数据范围

当题目明示或通过分析确定**元素值的范围**时（例如：元素值为 `0 ~ n-1`，或元素的绝对值 < n），马上考虑散列表。

> ⚠️ 必须确保 A[i] 不超出 count 下标范围；新建辅助数组后**一定初始化**！

---

#### 3. 预处理 ⭐（重难点）

> **核心思想**：类似散列表，以空间换时间。提前计算并存储结果，供后续快速查询。
> **使用条件**：需要频繁计算**区间和、积、最值**
> 常见有两类：前缀和、区间最值。

**前缀和**

```
sum[0] = 0
sum[1] = A[0]
sum[2] = A[0] + A[1] = sum[1] + A[1]
...
sum[i] = 前 i 个元素的和（下标 0 ~ i-1）
```

查询：`区间和 A[L, R] = sum[R + 1] - sum[L]` → O(1)

**区间最值**

B[i] 表示 A[0 ~ i]（共 i+1 个元素）的最值。预处理 O(n)，查询 O(1)。

---

#### 4. 多指针法

> **核心思想**：在一个或多个线性表上设置**两个或多个下标变量（指针）**，根据特定规则移动它们。
> ⭐ **「特定规则」是双指针法的灵魂。**

**三大要点**：① 指针起始位置 ② 移动规则 ③ 停止处理

**适用情形**：

| 情形 | 描述 |
|------|------|
| **多个线性表** | 每个表一个指针，如合并有序表 |
| **单个有序线性表** | 首尾双指针（相向而行），看作从切点分成两个逻辑子序列 |
| **单个无序顺序表** | 考频低，用于 Partition（快排一趟划分） |

---

## 第二部分：链表

### 一、链表与顺序表的比较

| | 共同点 | 不同点 |
|------|------|------|
| 结论 | 都是线性表。顺序表的很多方法在链表中仍可用：**散列表、多指针法、预处理**（主要前两者） | 链表**不可随机访问**，快排、折半查找不能用；单链表只能**单向访问**，需掌握快慢指针、头插法等技巧 |

**做题推荐流程**：`暴力解法 → 判断能否优化 → 优化解`

> 💡 对链表，当算法时间复杂度 O(n log n) 以上时，则可以优化。否则不需要。

> ⚠️ 若要求空间 O(1)，不能开辟新数组；若要求不可修改链表结构，不能随意修改元素顺序及插入删除。

---

### 二、链表的定义

**单链表**（data 为权值，next 指向下一个结点）：

```cpp
typedef struct LNode{
    ElemType data;                // 该结点的权值
    struct LNode *next;           // next 指向下一个结点
};
```

**双链表**（prior 指向上一个，next 指向下一个）：

```cpp
typedef struct LNode{
    ElemType data;                // 该结点的权值
    struct LNode *prior, *next;   // prior 指向上一个结点
};
```

---

### 三、链表的基本操作

#### 1. 删除结点 p

- ① 不要断链 ② p 要更新所指结点
- 需释放内存（C 语言 free(p)），考试中为方便可不写

```cpp
pre->next = p->next;
p = p->next;

// 带 free 的写法：
pre->next = p->next;
LNode* temp = p;
p = p->next;
free(temp);
```

#### 2. 将 p 插入 pre 后面

⚠️ **先后再前**：先处理 p->next，后处理 pre->next

```cpp
p->next = pre->next;
pre->next = p;
```

#### 3. 尾插法和头插法

| 方法 | 用途 | 特点 |
|------|------|------|
| **尾插法** | 保持插入顺序与最终顺序一致 | 每次在末尾插入，需 tail 指针 |
| **头插法** | 插入顺序与最终顺序相反 | 插入到头结点和第一个数据结点之间，常用于**逆置** |

> ✅ **口诀**：所有单链表插入都应先连后，再连前。这样一定不会断链。

#### 4. 遍历链表

- 元素确定用 for 循环，否则用 while 循环
- 每次 p = p->next
- 带头结点链表从 L->next 开始
- 可通过遍历统计元素个数 n

---

### 四、链表的暴力解

| 方法 | 说明 |
|------|------|
| **枚举法，直接处理** | 将需重排的元素一个一个拆下来重新插入 |
| **链表转数组，再处理** | 用数组保存结点地址或数据，再按数组方法处理 |

---

### 五、链表的优化解

#### 1. 多指针法

单链表中只考虑第一种（多个有序链表），此外还有：**快慢指针、前后指针**。

**① 多个有序链表**：归并两个带头结点的升序链表。

**② 快慢指针**：
- **判断环**：p 和 q 初始指向第 1 个结点，p 每次移 1，q 每次移 2。若 p == q，说明有环。
- **求中点**：q 走到终点时 p 在中点。可据此将链表一分为二。

**③ 前后指针**：p 指向 L，q 指向第 k 个结点。同时后移，q 指向 NULL 时 p 指向**倒数第 k 个元素**。

#### 2. 散列表

和数组中一样，额外定义数组保存中间过程，如 `count[n]` 表示链表中权值为 n 的元素出现次数。注意下标不要越界。

---

### 六、逆置

逆置即将线性表反转，顺序完全反过来。

| | 顺序表 | 链表 |
|------|------|------|
| **方法** | 首尾元素依次交换 | 头插法重新插入所有结点 |
| **时间复杂度** | O(n) | O(n) |
| **关键** | A[0]↔A[n-1], A[1]↔A[n-2]… | p 指向当前结点，temp 指向下一个，每次把 p 头插到 L 后面 |

---

### 七、经典真题

（见 Word 文档附图）

---

## 总结速查表

### 顺序表

| 方法 | 条件 | 时间 | 空间 | 关键点 |
|------|------|:----:|:----:|--------|
| 暴力枚举 | 无限制 | O(n²) 等 | O(1) | 遍历所有可能 |
| 快速排序 | 可改变下标 | O(n log n) | O(log n) | 不稳定，有使用限制 |
| 折半查找 | 有序表 | O(log n) | O(1) | 两个模板，边界查找用模板二 |
| 散列表 | 数据范围有限 | O(n) | O(m) | 空间换时间，注意初始化 |
| 预处理 | 频繁区间查询 | O(n) + O(1) | O(n) | 前缀和、区间最值 |
| 多指针法 | 线性表（有序） | 视情况 | O(1) | 灵魂在移动规则 |

### 链表

| 方法 | 适用条件 | 关键点 |
|------|----------|--------|
| 暴力枚举 | 无限制 | 拆下重插，或转数组处理 |
| 多指针法 | 有序链表为主 | 归并 + 快慢指针 + 前后指针 |
| 散列表 | 数据范围有限 | 同顺序表，注意下标范围 |
| 逆置 | 需要反转 | 链表用头插法，顺序表用首尾交换 |
| 头插法/尾插法 | 插入大量结点 | 先连后，再连前。头插逆序，尾插保序 |

---

> 📝 整理自桌面「算法题.docx」| 🖼️ 图片文件在 `算法题_images/word/media/` 目录下 | 📅 2026-08-02
